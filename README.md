# Ephix Pulse — Mobile

Native iOS/Android app for ephix.net/pulse, built with Expo + React Native + Expo Router.

**Start here:** read `SETUP.md` for step-by-step instructions (install → run in Expo Go → cloud builds).

Quick start:
```bash
npm install
npx expo start   # scan the QR with Expo Go on your phone
```
